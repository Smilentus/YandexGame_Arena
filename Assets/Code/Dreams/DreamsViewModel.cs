using UnityEngine;

namespace Dimasyechka.Code.Dreams
{
    public class DreamsViewModel : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
