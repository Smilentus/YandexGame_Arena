using UnityEngine;

namespace Dimasyechka.Code.CharacterSystem
{
    public class CharacterProfile : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
